# PRD_SYSTEM CENTRAL DOCKER COMPOSE CONFIGURATION
# Para ejecutar la infraestructura consolidada complete:
# docker-compose -f 01_ENTORNO/docker/docker-compose.yml up -d

include:
  - 01_ENTORNO/docker/docker-compose.yml
